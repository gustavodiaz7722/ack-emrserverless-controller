---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  creationTimestamp: null
  name: {{ include "ack-emrserverless-controller.app.fullname" . }}-writer
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ include "ack-emrserverless-controller.app.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    k8s-app: {{ include "ack-emrserverless-controller.app.name" . }}
    helm.sh/chart: {{ include "ack-emrserverless-controller.chart.name-version" . }}
rules:
- apiGroups:
  - emrserverless.services.k8s.aws
  resources:
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - emrserverless.services.k8s.aws
  resources:
  verbs:
  - get
  - patch
  - update
